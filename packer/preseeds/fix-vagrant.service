[Unit]
Description="Fixes vagrant from changing interfaces file and removing routes"
Before=network.target

[Service]
Type=simple
ExecStart="/bin/cp /etc/network/virtualbox-interfaces /etc/network/interfaces"

[Install]
WantedBy=multi-user.target
